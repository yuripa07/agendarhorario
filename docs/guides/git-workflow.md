# Workflow Git

- Trabalhe em `main` enquanto o projeto for solo.
- Use Conventional Commits em inglês e no imperativo.
- Antes de commit, rode `pnpm lint`, `pnpm typecheck` e `pnpm test`.
- Não reescreva histórico compartilhado sem combinar com Yuri.
